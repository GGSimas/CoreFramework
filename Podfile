# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

Pod::Spec.new do |s|
  s.name               = 'CoreFramework'
  s.version            = '1.0.0'
  s.summary            = 'Um framework para utilizar como core de design para Reminder Company'
  s.description        = <<- DESC
     Onboarding, Toggle de favoritos e avaliar na loja, além de um design System completo 

end
